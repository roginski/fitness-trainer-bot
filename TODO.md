# TODO

## UX

- [ ] `/open` flow for Telegram Desktop feels clunky — user must manually type `/open` to get a login button. Consider auto-sending the button on `/start` when `WEBAPP_URL` is set, or making the menu button itself trigger a token link somehow.
